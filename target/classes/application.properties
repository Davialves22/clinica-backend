# # # MYSQL
# # spring.datasource.url=jdbc:mysql://localhost:3306/demo_security?useTimezone=true &
# # serverTimezone=America/Sao_Paulo &
# # useSSL=false
# # spring.datasource.username=root
# # spring.datasource.password=root

# # # JPA
# # spring.jpa.hibernate.ddl-auto=none
# # spring.jpa.show-sql=true
# # spring.jpa.open-in-view=true
# # spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# #spring.datasource.url=${SPRING_DATASOURCE_URL}
# #spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
# #spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}

# #spring.jpa.hibernate.ddl-auto=update
# #spring.jpa.show-sql=true
# #spring.jpa.properties.hibernate.format_sql=true
# #spring.jpa.open-in-view=true

# # =============================
# # == POSTGRES (ENV VARS) ======
# # =============================
# # spring.datasource.url=${SPRING_DATASOURCE_URL}
# # spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
# # spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}
# #
# # spring.jpa.hibernate.ddl-auto=update
# # spring.jpa.show-sql=true
# # spring.jpa.properties.hibernate.format_sql=true
# # spring.jpa.open-in-view=true

# # =============================
# # == POSTGRES COM DOCKER ======
# # (SEM docker-compose) =========
# # docker run -d -p 5432:5432 -e POSTGRES_DB=demo_security -e POSTGRES_USER=postgres -e POSTGRES_PASSWORD=postgres postgres:15
# # =============================
# spring.datasource.url=jdbc:postgresql://localhost:5432/spring_security
# spring.datasource.username=root
# spring.datasource.password=root

# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.format_sql=true
# spring.jpa.open-in-view=true
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# # =============================
# # == POSTGRES COM COMPOSE =====
# # docker-compose up -d ========
# # =============================
# # spring.datasource.url=jdbc:postgresql://localhost:5432/demo_security
# # spring.datasource.username=postgres
# # spring.datasource.password=postgres
# #
# # spring.jpa.hibernate.ddl-auto=update
# # spring.jpa.show-sql=true
# # spring.jpa.properties.hibernate.format_sql=true
# # spring.jpa.open-in-view=true
# # spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

spring.application.name=spring_security

server.port=8080
spring.config.import=optional:file:.env[.properties]

# Datasource ( levantando a aplicação sem container )
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.url=jdbc:postgresql://localhost:5435/spring_security
spring.datasource.username=postgres
spring.datasource.password=root

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults=false
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQL9Dialect
spring.jpa.generate-ddl=true
